# 材料验证记录

日期：2026-09-12。对象：本课程的学习文件与教学演示。以下环境、编译及十项演示结果来自初版材料准备；后续“结合CODEX_WORKFLOW”的文档整合结果单列在文末。参考项目用于源码阅读，没有启动或压测完整cms-flow服务。

## 环境与命令

- 系统：Windows、PowerShell。
- JDK：Eclipse Temurin OpenJDK 21.0.11。
- `javac -encoding UTF-8 --release 21` 编译 AsyncLab.java 和 PracticeDag.java 成功。
- `run.ps1 -Case all` 最终运行退出码为0，十个演示均输出PASS。
- 单文件启动命令 `java ... AsyncLab.java threads` 已运行成功，可作为不运行PowerShell脚本时的入口。
- Java参数使用引号，并显式设置file/stdout/stderr为UTF-8，修正了初次验证发现的PowerShell参数解析和中文输出编码问题。

## 十项演示的实际结果

| 演示 | 已观察与检查的行为 |
|---|---|
| threads | 同一Runnable在main/worker执行；放行前Future未完成，放行后join返回 |
| saturation | 固定观察点3个线程、2个排队、1个拒绝 |
| discard | 静默丢弃没有抛提交异常，但Future仍未完成 |
| futures | 两项结果正常合并；先提交后汇合与逐项等待均正常运行 |
| dag | content先结束；goods/price第二波；assemble等两者结束后执行 |
| starvation | 父任务等待同池子任务超时；父退出后子获得线程 |
| failure | 一个Future失败而另一个未完成时allOf仍未完成；放行后传播原异常 |
| timeout | get超时不完成原Future；orTimeout完成Future但实际工作仍等待；放行后实际工作继续 |
| cancel | CompletableFuture取消后，底层演示任务仍继续且未收到该取消导致的中断 |
| virtual | 两个虚拟任务占满许可；第三个被拒绝；正常、运行失败、提交失败路径均归还许可 |

毫秒数未作为正确性硬阈值；吞吐、P95/P99及真实下游承载能力没有在这些本地实验中测量。

## 综合练习的状态

PracticeDag.java六个TODO仍由学习者完成。编译成功；运行 `run.ps1 -Case practice` 按预期报告 `UnsupportedOperationException: TODO 1`，并返回失败状态。

文件内已提供正常拓扑及同波并发、节点失败、环三个检查，**尚未在学习者完成的算法上通过**。这些检查只覆盖题目约定的输入与行为，不代表通用DAG引擎的完整测试集。

## 文档检查

- 初版共8个知识章节；时间相加为400分钟学习+70分钟休息+10分钟整理=480分钟。
- 检查Markdown本地链接指向文件是否存在，带行号的源码链接是否落在文件范围内。
- 检查讲义UTF-8文本；核心拒绝策略、SLOW装配、等待点、共享状态和超时配置已对照当前源码。
- 学习记录保持待验证。以上演示是教材验证，不是学习者掌握记录。

入口：[课程大纲](README.md)。

## 2026-09-12：结合CODEX_WORKFLOW的文档整合

- 新增桥接讲义，以及本地CODEX_WORKFLOW、docs/PLAN、docs/PROGRESS；修改课程入口、学习记录、自测和源码地图。当前为8个原知识章节加1份桥接讲义。
- 整合小时表保持S1—S8：接口与线程、注解与代理、原生池、Future/DAG、异步日志、失败超时、VT/信号量、配置关闭与迁移。
- 原DAG六TODO作为课后选做；M0—M6仍为后续工程里程碑，全部TODO，学习状态全部NOT_ASSESSED。
- 本轮只读核对FlowDag、FlowDagDefinition、FlowDagDefinitionCollector、NodeRequestParser和执行器工厂方法，支撑桥接讲义的实际调用链。
- 本轮验证范围是Markdown本地链接与行号、8段时间合计和工程状态表。没有修改Java或运行脚本，不重复运行十项演示，也没有创建或测试Spring日志项目。
- 既有演示不被追认为原任务书M4—M6验收：2/4/3参数、真实Spring行为及有界关闭等仍待后续实施验证。
