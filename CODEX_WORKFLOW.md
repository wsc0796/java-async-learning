# 整合工作流：扩展机制与线程池异步学习

> 2026-09-12整合版。当前工作目录：`.`。来源：[用户提供的原任务书](reference/OPERATION_LOG_WORKFLOW.md)与本地cms-flow学习包。

## 教材与当前学习请求

默认按README的S1—S8进行课程学习。原任务书中的M0启动语句是实践入口，不会因为阅读教材就成为执行命令。工程实践仅在学习者要求时进入，M0—M6验收以原任务书为准。进度文件是空白模板，实际进度由学习者提供。

## 统一学习主线

```text
业务需求
 → 找到扩展点
 → 我提供接口实现、注解或配置
 → 注册和装配
 → 框架在特定时机调用
 → 同步执行或提交任务
 → 观察结果、失败与资源结束
```

每次先问五个问题：我提供什么？谁使用它？何时使用？怎样接入？失败怎么办？出现异步时再填[八问卡](%E5%AD%A6%E4%B9%A0%E8%AE%B0%E5%BD%95.md)：调用线程、执行线程、Task、Executor、Future、等待点、共享状态、失败传播。

`cms-flow`用于真实实现取证。拟建`operation-log-lab`用于小步实现和迁移；文档中的设计类名不当成已存在的源码。

## 课程模式：S1—S8

1. 恢复时读README、PLAN、PROGRESS，以及当前课的学习记录；先确认当前目标和已存在的改动。
2. 每轮只教一个核心问题，精读两三个相关文件。先让学习者预测；明确表示不理解时，直接解释卡点并给小例子。
3. 使用现有实验或源码取证，不默认创建新功能。一次给一条与当前问题有关的命令，并解释目的。
4. 运行结果、学习者回答与设计推断分别记录。学习者没回答，不写已掌握；助手跑过实验不等于学员完成练习。
5. 当前问题讲解或验收结束后停下，说明下一问题。用户只问“检查答案”时，评估答案，不代写练习或进入新阶段。

每小时约50分钟学习；原知识章的全文用时不再逐章累加。第08知识章的小型DAG练习是整合路线的课后选做。

## 工程模式：M0—M6

进入工程模式后保留原任务书的逐阶段顺序：M0环境与骨架 → M1接口与同步服务 → M2注解反射 → M3同步AOP日志 → M4原生线程池 → M5异步日志 → M6配置与关闭。

- 当前课程目录已有学习包，后续工程拟放在`.\examples\operation-log-lab`，不重建或覆盖现有讲义与练习。
- M0开始时重新读取适用AGENTS、检查Git与实际工具链；在示例根保存原任务书、项目AGENTS、README、PLAN、PROGRESS、ENVIRONMENT及证据。此时才核实Maven和完整Spring Boot版本、创建最小构建。
- 每轮只完成一个工程里程碑。实现前明确行为与测试；运行适用检查、检查diff、保存真实证据后停止。
- 主链路只异步提交一次；@Async作为独立对照，不重复处理同一日志。业务返回不等Sink完成。
- 保留原返回值与原业务异常；日志普通故障独立观察，不吞严重Error。不可变记录只含必要信息，不采集业务敏感参数。
- 并发检查使用同步器与明确超时，释放阻塞任务，关闭并有界等待。既有演示不自动充当新工程的合格测试。
- 不自动提交或发布；对用户自己的实现与未提交改动先确认归属，不覆盖。

如果当前工程阶段尚未验证，后续“继续”先处理该阶段；验证后用户要求继续下一个阶段，可以推进。上一阶段未做学习验收则标记SKIPPED，不能改成UNDERSTOOD。用户明确改变节奏或范围时，以用户当前要求为准并记录变更。

## 状态分开维护

| 状态对象 | 状态与依据 |
|---|---|
| 材料验证 | 记录演示/链接等实际检查及日期，保存在材料验证记录；不冒充工程阶段通过 |
| 工程状态 | TODO / IN_PROGRESS / VERIFIED / BLOCKED；VERIFIED要求该里程碑验收及适用回归实际通过 |
| 学习状态 | NOT_ASSESSED / NEEDS_PRACTICE / UNDERSTOOD / SKIPPED；依据学习者可观察的回答和动手结果 |

当前M0—M6全部TODO；S1—S8学习状态全部NOT_ASSESSED。状态文件是恢复入口，后续仍须对照实际源码与证据，不能只相信标签。

## 每轮如何交付

用简短中文说明：本轮问题和状态；关键文件；一条真实调用链；实际命令与结果（未运行则说明）；一个预测与观察的对比；理解问题或小练习；进度位置与下一步。

课堂口头一次只问一个问题；每节末可留下三个复习问题供自行填写，不自动代答。工程阶段沿用原任务书的三个理解问题和一个动手练习。未完成学员练习与材料检查分离，不能故意让正常回归失败。

## 可直接使用的下一步指令

**开始或继续课程：**

> 读取当前目录的CODEX_WORKFLOW.md、docs/PLAN.md和docs/PROGRESS.md，继续当前S阶段的第一个未解决问题。先让我预测，一次只问一个问题，使用已有材料。依据我的回答记录学习状态。

**检查我的答案：**

> 检查我刚才的答案或练习改动，区分概念误解、代码问题和尚未验证的行为。可以运行相关检查；不要替我补答案或自动进入新阶段。

**开始项目实践：**

> 按原CODEX_WORKFLOW任务书和本地PLAN，在规划的独立示例目录只实施M0。重新核实环境，创建最小应用和真实映射测试，保存证据，交付后停止，不提前实施M1—M6。

**恢复已有工程阶段：**

> 先核对operation-log-lab的实际源码、任务书、PLAN、PROGRESS和最近证据，继续当前未完成的M阶段；本轮最多推进一个阶段，不覆盖我的改动。
